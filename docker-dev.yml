version: '3'
services:
  web:
    command: yarn start
    volumes:
      - ./client/src:/var/app/src
    ports:
      - "5000:3000"
  server:
    command: yarn start
    volumes:
      - ./server/src:/var/app/src
